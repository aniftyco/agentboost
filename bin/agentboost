#!/usr/bin/env node
import { AgentBoost } from '../dist/index.js';

const agentboost = new AgentBoost(process.cwd());

agentboost.run(process.argv.slice(2)).catch((err) => {
  console.error(err);
  process.exit(1);
});
